import { FormatRateDirective } from './format-rate.directive';

describe('FormatRateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatRateDirective();
    expect(directive).toBeTruthy();
  });
});
